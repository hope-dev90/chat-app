# Chat Backend

Express, PostgreSQL, JWT auth, mentor requests, and Socket.IO chat.

Useful files:

- `API.md` - endpoint and socket testing guide
- `schema.sql` - PostgreSQL database tables

Start locally:

```bash
npm install
npm run dev
```
